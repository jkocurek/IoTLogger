<%= form_with(model: room, local: true) do |form| %>
  <% if room.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(room.errors.count, "error") %> prohibited this room from being saved:</h2>

      <ul>
      <% room.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :room_name %>
  </div>

  <div class="field">
    <%= form.label :high %>
    <%= form.number_field :high, id: :room_high %>
  </div>

  <div class="field">
    <%= form.label :low %>
    <%= form.number_field :low, id: :room_low %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
